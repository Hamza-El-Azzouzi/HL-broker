{
    "private": true,
    "scripts": {
        "dev": "vite --port 3000",
        "build": "vite build",
        "start": "concurrently \"php artisan serve\" \"cd react && npm run dev\""
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    },
    "dependencies": {
        "concurrently": "^8.0.1"
    }
}
